#include<stdio.h>
int main()
{
	int j=4,k;
	
	k=!5 && j;
	
	printf("%d",k); //op 0 becuase !5 means non-zero cha zero jhala to so o && 4 mhnje zero and mule or asta tr 1 aala asta
	
	return 0;
}
